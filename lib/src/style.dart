class AppStyle {
  static const spacing = 16.0;

  static const hSpacing = spacing;
  static const hSpacingHalf = hSpacing / 2;

  static const vSpacing = spacing;
  static const vSpacingHalf = vSpacing / 2;
}
